buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.google.gms:google-services:4.0.1'
    }
}

android {
    defaultConfig {
        ndk {
            abiFilters 'armeabi-v7a', 'x86'
        }
    }
}

repositories {
    maven {
      url 'https://s3.amazonaws.com/sightcall.maven.release'
    }
}

dependencies {
    implementation 'com.sightcall.universal:universal-sdk:4.0.0-mapfre-support'
}

ext.postBuildExtras = {
    apply plugin: 'com.google.gms.google-services'
}